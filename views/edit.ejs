<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 px-3 py-2 rounded-md text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind?" class="p-3 w-1/3 block outline-none resize-none bg-trasnparent rounded-md border-2 border-zinc-800" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 w-40 bg-yellow-500 text-black block rounded-md mt-2" type="submit" value="Done">
        </form>
    </div>
</body>
</html>